
function retornaMapsbyUsers(usertype){
    //retorna usertypes 
    mapyType=managerSite.layerId
  }
  function retornaProjetosbyUsers(usertype){
    if(usertype==='Topografia' || usertype=== 'Comercial Fundiario'){
      const usersComercialFundiario=managerSite.tableId
      const usersTopografia=managerSite.tableId
      const ProjetoResponsavel=managerSite.tableId
  
    //Fazer join com os 3 elementos em userTopografia
    //limitar por usertype o json
    const Projetos=(usertype)
    //Depois usa Projetos pra fazer uma operacao com os projetos permitidos
    returnProjetos(portal)//Fazer operacao com o projetos anteriores
    }
    else {
      returnProjetos(portal)
    }
    
  }
  function columnsbyUsers(usertype){
    const usersComercialFundiario=managerSite.tableId;
    const usersTopografia=managerSite.tableId;
    const invisibleColumns=managerSite.tableId;
  
  }
  function retornaTablebyUsers(usertype){
    if(usertype==='Topografia' || usertype=== 'Comercial Fundiario'){
      const usersComercialFundiario=managerSite.tableId
      const usersTopografia=managerSite.tableId
      const ProjetoResponsavel=managerSite.tableId
  
    //Fazer join com os 3 elementos em userTopografia
    //limitar por usertype o json
    const Projetos=(usertype)
    //Depois usa Projetos pra fazer uma operacao com os projetos permitidos
    returnProjetos(portal)//Fazer operacao com o projetos anteriores
    }
    else {
      returnProjetos(portal)
    }
  }
